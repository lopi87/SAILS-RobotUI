<style>
  .color_event_<%= event.id %> {
    background-color: <%= event.color %> !important;
  }

  .event_<%= event.id %>.position_<%= event.id %>{
    left: <%= event.coordinate_x + 'px'%>;
    top: <%= event.coordinate_y + 'px'%>;
    position: absolute;
  }
</style>
