<style>
  .play_<%= video.id %> {
  }

  .play_<%= video.id %>.position_<%= video.id %>{
    left: <%= video.coordinate_x + 'px'%>;
    top: <%= video.coordinate_y + 'px'%>;
    position: relative;
  }
</style>
